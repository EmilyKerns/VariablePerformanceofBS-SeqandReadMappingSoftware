#!/bin/bash

# Replace REFERENCE with your reference genome and /PATH/BSBolt with the directory you want to store the indexed reference genome in. Can compress the reference into tar.gz before running alignment script
bsbolt Index -G REFERENCE -DB /PATH/BSBolt
